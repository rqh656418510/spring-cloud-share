server:
  port: 8091
  servlet:
    context-path: /uaa

spring:
  application:
    name: auth-server

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8090/eureka
  instance:
    prefer-ip-address: true